<%- include('partials/header') %>

<div class="shop-head">
  <div class="container">
    <nav class="crumb"><a href="/">Home</a> <span>/</span> <% if (parentCat) { %><a href="/categorie/<%= parentCat.slug %>"><%= parentCat.name %></a> <span>/</span> <% } %><span class="cur"><%= category.name %></span></nav>
    <h1 class="shop-title"><%= category.name %></h1>
    <% if (category.blurb) { %><p class="shop-blurb"><%= category.blurb %></p><% } %>

    <% if (children && children.length) { %>
      <div class="subcat-row">
        <% children.forEach(function(c){ %>
          <a href="/categorie/<%= c.slug %>" class="chip"><%= c.name %></a>
        <% }) %>
      </div>
    <% } %>
  </div>
</div>

<section class="sec" style="background:var(--black);padding-top:36px;">
  <div class="container">
    <% if (catBrands && catBrands.length > 1) { %>
      <div class="filter-row" style="margin-top:0;margin-bottom:26px;">
        <span class="filter-lbl">Merk:</span>
        <a href="/categorie/<%= category.slug %>" class="chip <%= !activeBrand ? 'chip-on' : '' %>">Alle</a>
        <% catBrands.forEach(function(b){ %>
          <a href="/categorie/<%= category.slug %>?merk=<%= encodeURIComponent(b) %>" class="chip <%= activeBrand===b ? 'chip-on' : '' %>"><%= b %></a>
        <% }) %>
      </div>
    <% } %>

    <p class="result-count"><%= products.length %> product<%= products.length===1?'':'en' %><% if (activeBrand) { %> van <strong><%= activeBrand %></strong><% } %></p>

    <% if (products.length) { %>
      <div class="pg">
        <% products.forEach(function(p){ %>
          <%- include('partials/product-card', { p: p }) %>
        <% }) %>
      </div>
    <% } else { %>
      <p class="empty-msg">Geen producten gevonden in deze categorie.</p>
    <% } %>
  </div>
</section>

<%- include('partials/footer') %>
