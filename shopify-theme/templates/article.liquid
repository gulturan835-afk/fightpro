<section class="block wrap" style="max-width:760px">
  <div class="section-head"><div><span class="eyebrow">{{ article.published_at | date: '%d %b %Y' }}</span><h2>{{ article.title }}</h2><div class="scan-line"></div></div></div>
  <div class="rte">{{ article.content }}</div>
</section>
