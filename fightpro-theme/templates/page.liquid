<div class="shop-head">
  <div class="container">
    <nav class="crumb"><a href="{{ routes.root_url }}">Home</a> <span>/</span> <span class="cur">{{ page.title }}</span></nav>
    <h1 class="shop-title">{{ page.title }}</h1>
  </div>
</div>

<section class="sec" style="background:var(--black);padding-top:40px;">
  <div class="container" style="max-width:840px;">
    <div class="ab-p">{{ page.content }}</div>
  </div>
</section>
