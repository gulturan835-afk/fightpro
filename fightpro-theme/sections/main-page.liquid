<div class="shop-head">
  <div class="container">
    <nav class="crumb" aria-label="Kruimelpad">
      <a href="{{ routes.root_url }}">{{ 'general.breadcrumb.home' | t }}</a>
      <span>›</span><span class="cur">{{ page.title }}</span>
    </nav>
    <h1 class="shop-title">{{ page.title }}</h1>
  </div>
</div>

<section class="sec-sm">
  <div class="container">
    <div class="pd-desc" style="max-width:760px">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Pagina", "settings": [] }
{% endschema %}
